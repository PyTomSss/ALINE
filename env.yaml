name: aline

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip

  - numpy
  - scipy
  - pandas
  - matplotlib
  - tqdm
  - scikit-learn

  - pip:
      - termcolor
      - wandb
      - hydra-core
      - attrdictionary
      - gradnorm-pytorch
      - ipykernel